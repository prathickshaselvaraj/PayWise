<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PayWise"
        tools:targetApi="31">

        <!-- Splash Activity (Launcher) -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.PayWise.Splash">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Account Setup Activity -->
        <activity
            android:name=".activities.AccountSetupActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Bank Details Activity -->
        <activity
            android:name=".activities.BankDetailsActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- PIN Setup Activity -->
        <activity
            android:name=".activities.PinSetupActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Profile Setup Activity -->
        <activity
            android:name=".activities.ProfileSetupActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Login Activity -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Main Activity (Dashboard) -->
        <!-- ✅ FIX: Your MainActivity is com.example.paywise.MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Payment Entry Activity -->
        <activity
            android:name=".activities.PaymentEntryActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Vault Selection Activity -->
        <activity
            android:name=".activities.VaultSelectionActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Payment Confirmation Activity -->
        <activity
            android:name=".activities.PaymentConfirmationActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Vault Management Activity -->
        <activity
            android:name=".activities.VaultManagementActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Transaction History Activity -->
        <activity
            android:name=".activities.TransactionHistoryActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Services -->

        <!-- Vault Monitor Service -->
        <service
            android:name=".services.VaultMonitorService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Payment Validation Service -->
        <service
            android:name=".services.PaymentValidationService"
            android:exported="false" />

        <!-- Broadcast Receivers -->

        <!-- Payment Alert Receiver -->
        <receiver
            android:name=".receivers.PaymentAlertReceiver"
            android:exported="false">

            <intent-filter>
                <action android:name="com.example.paywise.action.DAILY_CHECK" />
                <action android:name="com.example.paywise.action.MONTHLY_RESET" />
                <action android:name="com.example.paywise.action.LIMIT_ALERT" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>

        </receiver>

    </application>

</manifest>
