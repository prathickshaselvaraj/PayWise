<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PayWise"
        tools:targetApi="31">

        <!-- Splash Activity (Launcher) -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Profile Setup Activity -->
        <activity
            android:name=".activities.ProfileSetupActivity"
            android:exported="false"
            android:screenOrientation="portrait"/>

        <!-- Main Activity -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false"
            android:screenOrientation="portrait"/>

        <!-- Vault Management Activity -->
        <activity
            android:name=".activities.VaultManagementActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity"
            android:screenOrientation="portrait"/>

        <!-- Payment Activity -->
        <activity
            android:name=".activities.PaymentActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity"
            android:screenOrientation="portrait"/>

        <!-- Transaction History Activity -->
        <activity
            android:name=".activities.TransactionHistoryActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity"
            android:screenOrientation="portrait"/>

        <!-- Background Service - Vault Monitor -->
        <service
            android:name=".services.VaultMonitorService"
            android:enabled="true"
            android:exported="false"/>

        <!-- Foreground Service - Payment Validation -->
        <service
            android:name=".services.PaymentValidationService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>

        <!-- Broadcast Receiver - Payment Alerts -->
        <receiver
            android:name=".receivers.PaymentAlertReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.paywise.LOW_BALANCE"/>
                <action android:name="com.example.paywise.VAULT_RESET"/>
                <action android:name="com.example.paywise.PAYMENT_FAILED"/>
            </intent-filter>
        </receiver>

    </application>

</manifest>